<% include ../_partials/nav_dashboard.ejs %>

<div class="card bg-light mb-3 mx-auto mt-5" style="max-width: 18rem; margin-top: 20px; border-radius: 10px;">
    <div class="card-header">
        Ajouter d'un site
	</div>
	<div class="card-body">
        <form action="/admin/sites/update/<%= site.id %>" method="POST">
            <div class="form-group">
                <label for="nom-categorie">Nom du site :</label>
				<input type="text" class="form-control" name="nom_site" id="nom-categorie" value="<%= site.nom_site %>" />
			</div>
			<!-- <div class="form-group">
                <label for="active-categorie">Statut :</label>
				<select value="<%= site.statut %>" name="statut" class="form-control" id="active-categorie" >
                    <option value="0">Inactif</option>
					<option value="1">Actif</option>
				</select>
			</div> -->
            
            <div class="card-footer bg-transparent border-success justify-content-center d-flex">
                <button type="submit" class="btn btn-warning">Modif</button>
            </div>
		</form>
	</div>
</div>

        <% include ../layouts/doctype_end.ejs %>
